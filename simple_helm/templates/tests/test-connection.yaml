apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simple_helm.fullname" . }}-test-connection"
  labels:
    {{- include "simple_helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simple_helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
